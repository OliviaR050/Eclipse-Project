# Eclipse-Project
This allows a user to create a username and password and then login through a different window. The users sign-in information is stored in a database from an SQL query. Java Swing is used to create the login and sign up screen. It enforces the user to repeat the password twice when signing up and makes sure the letters are concealed as dots.
